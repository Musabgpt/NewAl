package com.musab.aragpt2;

/** Default chat mode for the app. Normal conversation must not force a coding persona. */
final class GenerationMode {
    static final boolean DEFAULT_CODE_MODE = false;

    private GenerationMode() {}
}
