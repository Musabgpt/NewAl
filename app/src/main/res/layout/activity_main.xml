<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="rtl">

    <LinearLayout
        android:id="@+id/rootView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/bg">

        <!-- Top bar: menu, the model (tap to change), new chat. -->
        <LinearLayout
            android:id="@+id/headerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:paddingTop="4dp"
            android:paddingBottom="2dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/menuButton"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:gravity="center"
                    android:text="☰"
                    android:textColor="@color/text_primary"
                    android:textSize="22sp"
                    android:background="?android:attr/selectableItemBackgroundBorderless"/>

                <Button
                    android:id="@+id/loadModelButton"
                    style="?android:attr/borderlessButtonStyle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:text="NewAl ▾"
                    android:textAllCaps="false"
                    android:textColor="@color/text_primary"
                    android:textSize="17sp"
                    android:textStyle="bold"/>

                <TextView
                    android:id="@+id/clearButton"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:gravity="center"
                    android:text="✎"
                    android:textColor="@color/text_primary"
                    android:textSize="22sp"
                    android:background="?android:attr/selectableItemBackgroundBorderless"/>
            </LinearLayout>

            <TextView
                android:id="@+id/status"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:maxLines="1"
                android:ellipsize="end"
                android:paddingBottom="2dp"
                android:text="اختر نموذجاً من الأعلى"
                android:textColor="@color/text_secondary"
                android:textSize="11sp"/>
        </LinearLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/chatList"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingTop="4dp"
                android:paddingBottom="8dp"
                android:clipToPadding="false"/>

            <ScrollView
                android:id="@+id/emptyView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone">
                <LinearLayout
                    android:id="@+id/examples"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="20dp"/>
            </ScrollView>
        </FrameLayout>

        <!-- Chips of the phone assistant / Termux agent modes. -->
        <HorizontalScrollView
            android:id="@+id/agentBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            android:visibility="gone">
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/agentChips"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                app:singleLine="true"
                app:chipSpacingHorizontal="6dp"/>
        </HorizontalScrollView>

        <!-- The input box, ChatGPT style: attach, text, microphone, send; switches below it. -->
        <LinearLayout
            android:id="@+id/inputBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/input_bg"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp">

            <EditText
                android:id="@+id/inputBox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                android:gravity="start|center_vertical"
                android:hint="اسأل أي شيء"
                android:inputType="textMultiLine|textCapSentences"
                android:maxLines="6"
                android:minHeight="44dp"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"
                android:textDirection="firstStrong"
                android:textSize="16sp"/>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/attachButton"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:gravity="center"
                    android:text="＋"
                    android:textColor="@color/text_primary"
                    android:textSize="20sp"
                    android:background="?android:attr/selectableItemBackgroundBorderless"/>

                <HorizontalScrollView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:scrollbars="none">
                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal">
                        <Button android:id="@+id/webButton" style="@style/ModeChip" android:text="🌐 بحث"/>
                        <Button android:id="@+id/thinkButton" style="@style/ModeChip" android:text="💭 تفكير"/>
                        <Button android:id="@+id/assistantButton" style="@style/ModeChip" android:text="📱 الهاتف"/>
                        <Button android:id="@+id/agentButton" style="@style/ModeChip" android:text="🛠 Termux"/>
                    </LinearLayout>
                </HorizontalScrollView>

                <TextView
                    android:id="@+id/micButton"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:gravity="center"
                    android:text="🎙"
                    android:textSize="18sp"
                    android:background="?android:attr/selectableItemBackgroundBorderless"/>

                <Button
                    android:id="@+id/sendButton"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="4dp"
                    android:background="@drawable/send_bg"
                    android:minWidth="0dp"
                    android:minHeight="0dp"
                    android:padding="0dp"
                    android:text="↑"
                    android:textColor="#111318"
                    android:textSize="20sp"
                    android:textStyle="bold"/>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Side panel: conversations and settings. -->
    <LinearLayout
        android:id="@+id/drawerPanel"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/drawer"
        android:orientation="vertical"
        android:paddingTop="32dp">

        <Button
            android:id="@+id/drawerNewChat"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="start|center_vertical"
            android:paddingStart="18dp"
            android:text="✎  محادثة جديدة"
            android:textAllCaps="false"
            android:textColor="@color/text_primary"
            android:textSize="16sp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="18dp"
            android:paddingTop="10dp"
            android:paddingBottom="4dp"
            android:text="المحادثات"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"/>

        <ListView
            android:id="@+id/conversationList"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:divider="@null"/>

        <Button android:id="@+id/drawerPermissions" style="@style/DrawerItem" android:text="🔓  الصلاحيات الكاملة"/>
        <Button android:id="@+id/drawerSpeed" style="@style/DrawerItem" android:text="⚡  ضبط السرعة لهاتفك"/>
        <Button android:id="@+id/drawerModels" style="@style/DrawerItem" android:text="🧠  النماذج والأدوار"/>
        <Button android:id="@+id/drawerSettings" style="@style/DrawerItem" android:text="⚙  الإعدادات"/>
        <Button android:id="@+id/drawerClearAll" style="@style/DrawerItem" android:text="🗑  حذف كل المحادثات"/>
    </LinearLayout>
</androidx.drawerlayout.widget.DrawerLayout>
